spring:
  datasource:
    url: jdbc:h2:mem:service_insight_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
  flyway:
    locations: classpath:db/migration,classpath:db/vendor/h2

app:
  identity:
    session-ttl: PT8H
