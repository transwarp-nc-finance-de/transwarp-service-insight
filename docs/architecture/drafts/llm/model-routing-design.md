# 模型路由设计

## 目的

本文档描述后续目标架构中的多模型路由、版本标记、A/B 测试和回滚策略。

## 适用范围

适用于 LLM 推理服务工程化评审。当前阶段不部署真实模型。

## 正文

模型路由可根据意图类型、问题复杂度、检索结果质量、用户权限和安全风险选择不同模型。简单咨询可使用轻量模型，复杂诊断可使用更强模型。

每次生成应记录模型名称、版本、Prompt 模板版本、检索引用版本和安全拦截状态，便于审计和效果对比。

A/B 测试仅能在人工审核和安全评审通过后进行。模型异常时必须支持回滚或降级为仅展示检索结果。

