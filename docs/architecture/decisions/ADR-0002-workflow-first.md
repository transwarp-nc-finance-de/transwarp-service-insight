# ADR-0002：Workflow-first，Agent-ready

Status: ACCEPTED  
Owner: 架构负责人  
Last reviewed: 2026-07-12

## 决策

预诊主链路由代码控制的确定性工作流和状态机编排。Agent 只能作为未来可替换执行器，不是核心依赖。

## 约束

未来 Agent 必须使用白名单只读工具、结构化输出、固定轮次、超时和成本预算，并可完整回放。任何生产写操作、自动提交、最终根因或最终方案生成均禁止。只有相对单工作流的受控评估证明明显收益后才可进入主链路。
