# 数据脱敏设计

## 目的

本文档定义后续真实数据进入预诊、检索、生成和日志前的脱敏要求。

## 适用范围

适用于真实知识源、历史 SLA、日志摘要、告警摘要和演示样例制作。当前 Demo 仅使用 `模拟数据`。

## 正文

脱敏对象包括 IP、手机号、邮箱、密钥、Token、密码、真实日志路径、客户名称、项目名称、集群名称、SLA 编号、工单编号和内部链接。

示例：`模拟数据：客户 A 的 10.0.0.1 节点出现错误` 应脱敏为 `模拟数据：某客户的 <IP> 节点出现错误`。

任何基于真实材料制作的样例都必须先完成授权、脱敏和人工审核。

