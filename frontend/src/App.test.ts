import {flushPromises,mount} from '@vue/test-utils';import {describe,expect,it,vi} from 'vitest';import App from './App.vue';
describe('App',()=>{it('keeps manual submit available when precheck service fails',async()=>{vi.stubGlobal('fetch',vi.fn().mockRejectedValue(new Error('服务不可用')));const wrapper=mount(App);await wrapper.get('form').trigger('submit');await flushPromises();expect(wrapper.text()).toContain('可以继续人工提交');expect(wrapper.get('button.secondary').attributes('disabled')).toBeUndefined();await wrapper.get('button.secondary').trigger('click');expect(wrapper.text()).toContain('未调用真实提交接口')})});
